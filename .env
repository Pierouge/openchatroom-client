# NOTE: Since most of these values are transferred to the appsettings.json, and thus, to the client,
# no confidential data should be written here

# Note your url here if you want a default server to be chosen, otherwise keep it blank
API_URL=https://localhost:5443
